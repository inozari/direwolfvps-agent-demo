# Publish checklist

Before publishing this repository to GitHub:

- Replace placeholders with the official GitHub organization/user.
- Run `npm run check`.
- Confirm no `.env` file is committed.
- Add screenshots only if they do not reveal API tokens, wallets or IPs.
- Link back to https://direwolfvps.com/agents and https://direwolfvps.com/openapi.json.
